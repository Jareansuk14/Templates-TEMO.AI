---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { SITE, BRAND_NAME, EXTERNAL_LINK } from "@/config/site";
import { getPromotionSeoHeading, getPromotionSeoDescription } from "@/lib/seo-content";
import { PROMOS } from "@/lib/images";

const promotionSeo = getPromotionSeoHeading();
const pageDescription = getPromotionSeoDescription();

const promoKeywords: string[] = [];

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  name: `โปรโมชั่น ${SITE.name}`,
  description: pageDescription,
  url: `${SITE.url}/promotions/`,
  numberOfItems: PROMOS.length,
};
---

<BaseLayout
  title={`โปรโมชั่น ${SITE.name} | ${promotionSeo}`}
  description={pageDescription}
  keywords={[SITE.name, ...promoKeywords].join(", ")}
  jsonLd={jsonLd}
>

  <!-- PAGE HEADER -->
  <section class="page-header">
    <div class="container">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/">หน้าแรก</a>
        <span class="breadcrumb-sep" aria-hidden="true">/</span>
        <span aria-current="page">โปรโมชั่น</span>
      </nav>
      <h1 id="promotion-seo"><span class="section-brand">{BRAND_NAME}</span>โปรคาสิโนออนไลน์ จัดหนัก โบนัสคืนยอดรายวัน สมาชิกใหม่คุ้ม เงื่อนไขชัด ชวนเพื่อนมีรางวัล</h1>
      <p id="sub-promotion-seo">โปรโมชันของ MOOYIM99 ถูกออกแบบให้คุ้มค่าและตรงสไตล์การเล่น ตั้งแต่โบนัสต้อนรับ เครดิตทดลอง ภารกิจรายวันรายสัปดาห์ ไปจนถึงโปรคืนยอดตามระดับสมาชิก เงื่อนไขระบุชัด เลือกเปิดรับได้เอง ระบบเว็บตรงคำนวณและลงโบนัสอัตโนมัติ ตรวจสอบได้ในประวัติธุรกรรม ผู้เล่น คาสิโนออนไลน์ สล็อต บาคาร่า และแทงบอล จะเห็นข้อเสนอเฉพาะหมวด เพิ่มโอกาสทำกำไร มีโปรชวนเพื่อนและสิทธิ์วีไอพีพร้อมของรางวัลพิเศษ</p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="promo-grid-5">
        {PROMOS.map(({ src, alt, width, height }, i) => (
          <a
            href={EXTERNAL_LINK}
            class="promo-full-card"
            aria-label={alt}
          >
            <img
              src={src}
              alt={alt}
              width={width}
              height={height}
              loading={i < 2 ? "eager" : "lazy"}
              decoding="async"
            />
          </a>
        ))}
      </div>
    </div>
  </section>

</BaseLayout>
