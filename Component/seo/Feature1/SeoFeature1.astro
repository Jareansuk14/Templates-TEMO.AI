---
import { ACTION_BUTTONS, SEO_ARTICLE_IMAGES } from "@/lib/images";

interface Props { externalLink: string; }
const { externalLink } = Astro.props;

const playBtn = ACTION_BUTTONS.register;

const whySections = [
  { id: "seo-1", heading: "สล็อต เว็บตรง แตกหนัก RTP ชัด โบนัสจัดเต็ม ฝากถอนออโต้ รองรับวอลเล็ต จ่ายไว", desc: "ที่ TEMO888 คุณจะพบสล็อตจากค่ายชั้นนำพร้อมข้อมูลสำคัญครบ เช่น RTP ช่วงความผันผวน จำนวนเพย์ไลน์ และฟีเจอร์พิเศษ ตรวจสอบได้ก่อนหมุนจริง ลองเล่นโหมดเดโมเพื่อจับจังหวะและวางแผนงบอย่างมีวินัย จากนั้นค่อยปรับเบทตามกลยุทธ์ ฝากถอนออโต้ผ่านธนาคารและวอลเล็ต ทำรายการเร็ว ปลอดภัย และไม่เด้งออกจากหน้าเกม แยกหมวดชัดเจนทั้งคลาสสิก เมกะเวย์ ซื้อฟรีสปิน และแจ็กพอตสะสม หนึ่งบัญชีเชื่อม คาสิโนออนไลน์ สล็อต บาคาร่า แทงบอล บนเว็บตรง โปรรายวัน ภารกิจ และสะสมแต้มช่วยต่อยอดกำไร" },
];
---

{whySections.map(({ id, heading, desc }) => {
  const image = SEO_ARTICLE_IMAGES[id];
  return (
  <section id={id} class="why-feature-section" aria-labelledby={`${id}-title`}>
    <div class="container"><div class="why-feature-grid">
      <a href={externalLink} class="why-feat-img-wrap" aria-label={image.alt}><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /></a>
      <div class="why-feat-content">
        <h2 id={`${id}-title`}>{heading}</h2>
        <p id={`sub-${id}`}>{desc}</p>
        <a href={externalLink} class="hero-action-link" aria-label={playBtn.alt}><img src={playBtn.src} alt={playBtn.alt} width={playBtn.width} height={playBtn.height} loading="lazy" decoding="async" /></a>
      </div>
    </div></div>
  </section>
  );
})}
