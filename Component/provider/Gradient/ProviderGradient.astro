---
import { PROVIDER } from "@/lib/images";
---

<section class="prov-grad" aria-labelledby="providers-heading">
  <div class="container prov-grad-panel">
    <header class="prov-grad-head"><h2 id="providers-heading">ค่ายเกมชั้นนำที่ให้บริการ</h2></header>
    <img src={PROVIDER.src} alt={PROVIDER.alt} width={PROVIDER.width} height={PROVIDER.height} loading="lazy" decoding="async" />
  </div>
</section>
