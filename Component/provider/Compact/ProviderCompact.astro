---
import { PROVIDER } from "@/lib/images";
---

<section class="prov-cp" aria-labelledby="providers-heading">
  <div class="container">
    <h2 id="providers-heading" class="prov-cp-h">ค่ายเกมชั้นนำที่ให้บริการ</h2>
    <img src={PROVIDER.src} alt={PROVIDER.alt} width={PROVIDER.width} height={PROVIDER.height} loading="lazy" decoding="async" />
  </div>
</section>
