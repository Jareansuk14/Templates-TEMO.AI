---
import { PROVIDER } from "@/lib/images";
---

<section class="prov-bd" aria-labelledby="providers-heading">
  <div class="container">
    <header class="prov-bd-head"><h2 id="providers-heading">ค่ายเกมชั้นนำที่ให้บริการ</h2></header>
    <div class="prov-bd-frame">
      <img src={PROVIDER.src} alt={PROVIDER.alt} width={PROVIDER.width} height={PROVIDER.height} loading="lazy" decoding="async" />
    </div>
  </div>
</section>
