---
import { GAME_CARDS } from "@/lib/images";

interface Props { externalLink: string; }
const { externalLink } = Astro.props;

const cards = GAME_CARDS;
---

<section class="games-mascot" aria-label="เกมที่เราให้บริการ">
  <div class="games-mascot-inner" data-count={cards.length}>
    {cards.map(({ alt, src, width, height }) => (
      <a href={externalLink} class="game-mascot-card" aria-label={alt}>
        <img src={src} alt={alt} width={width} height={height} loading="lazy" decoding="async" />
      </a>
    ))}
  </div>
</section>
